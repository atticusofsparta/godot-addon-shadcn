---
title: Item
description: Media + title + description + actions row.
---

Media + title + description + actions row.

<ExampleFrame src="/godot-addon-shadcn/example/index.html?c=item" />

Use the panel on the left of the demo to customize this component live, and the
toolbar to switch base color, accent and light/dark.

## Usage

```gdscript
var it := ShadcnItem.new()
it.icon_text = "📁"
it.title = "Documents"
it.description = "12 files"
```
